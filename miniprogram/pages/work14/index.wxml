<swiper>
  <swiper-item wx:for="{{imglist}}" wx:key="unique">
    <image src="{{item.fileid}}" bindtap="vote" data-fileid="{{item.fileid}}" />
    <text>{{item.count}}</text>
  </swiper-item>
</swiper>